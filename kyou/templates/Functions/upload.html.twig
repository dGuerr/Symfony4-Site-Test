{% extends '../base.html.twig' %}

{% block body %}
    <h1>Adding a new photo</h1>

    {{ form_start(form) }}
    {{ form_row(form.photo) }}
    <button type="submit">Envoyer</button>
    {{ form_end(form) }}
{% endblock %}